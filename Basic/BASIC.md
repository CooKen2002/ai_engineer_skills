### Tầng 0 — Nền tảng lập trình & tư duy (bắt buộc tuyệt đối)

- **Python vững**: cấu trúc dữ liệu, giải thuật, OOP, xử lý file/JSON, viết code sạch, debug tốt
- **Git**: clone, branch, commit, PR, resolve conflict
- **Linux/terminal**: cài đặt môi trường, quản lý package (pip, venv/conda)
- **Đọc hiểu tài liệu kỹ thuật/paper** (kể cả không tự tin tiếng Anh nói, đọc paper vẫn phải ổn)